//
// Copyright (c) Purav Manot
//

import Foundation
import PDFKit
/*
func checkTopic(_ question: Question, topic: Topic) -> Bool {
    let extractedText: String = question.paper.pdf.extractText(pages: question.pages)
    var average: Int = 0
    for keyword in topic.keywords {
        let regex = try! NSRegularExpression(pattern: keyword, options: [.caseInsensitive])
        average += regex.matches(extractedText)
    }
    if average > 0 {
        return true
    }
    return false
}
*/
